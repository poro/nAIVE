# nAIVE Engine — Environment Variables
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Then source it before running:
#   source .env

# =============================================================================
# SLANG SDK (required for building)
# =============================================================================
# Path to the vendored SLANG SDK. The build system also reads this from
# .cargo/config.toml, so this is only needed if running outside of cargo.
export SLANG_DIR="vendor"

# =============================================================================
# AI Asset Generation — H100 Gateway (primary, recommended)
# =============================================================================
# Your own GPU server running the Hunyuan3D gateway API.
# Requires SSH tunnel: ssh -L 8000:localhost:8000 -p 2222 ubuntu@<your-server-ip>
#
# export GATEWAY_URL="http://localhost:8000"
# export GATEWAY_KEY="your-api-key"

# =============================================================================
# AI Asset Generation — HuggingFace Spaces (fallback)
# =============================================================================
# HuggingFace API token for 2D image generation (FLUX.1) and 3D model spaces.
# Get yours at: https://huggingface.co/settings/tokens
#
# export HF_TOKEN="hf_your_token_here"

# HuggingFace Space for 3D model generation.
# Duplicate one of these to your own account for dedicated GPU quota:
#   - https://huggingface.co/spaces/tencent/Hunyuan3D-2mini-Turbo
#   - https://huggingface.co/spaces/tencent/Hunyuan3D-2
# Then set MODEL_SPACE to "your-username/SpaceName"
#
# export MODEL_SPACE="your-username/Hunyuan3D-2mini-Turbo"

# Write token for pushing fixes to your duplicated HF Space (optional).
# Only needed if you're modifying the space's code.
#
# export HF_WRITE="hf_your_write_token_here"

# =============================================================================
# AI Asset Generation — Meshy AI (alternative)
# =============================================================================
# Get your key from: https://app.meshy.ai
#
# export MESHY_API_KEY="your-meshy-key"
